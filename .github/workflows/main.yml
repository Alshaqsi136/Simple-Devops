name: Docker CI/CD with Email Notification

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: Docker Compose Build
        run: docker compose build

      - name: Docker Compose Deploy
        run: docker compose up -d

  notify:
    runs-on: ubuntu-latest
    needs: build-deploy
    steps:
      - name: Send Success Email
        if: success()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: alshaqsi136@gmail.com
          password: "vpml fvst kezt klrc"
          subject: "Build & Deploy Success ✅"
          body: "It worked in production just like in local host !!"
          to: nasseor4@gmail.com
          from: alshaqsi136@gmail.com
          secure: true

      - name: Send Failure Email
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: alshaqsi136@gmail.com
          password: "vpml fvst kezt klrc"
          subject: "Build & Deploy Failed ❌"
          body: "Docker build or deploy failed. Check the workflow logs!"
          to: nasseor4@gmail.com
          from: alshaqsi136@gmail.com
          secure: true
